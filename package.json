{
  "name": "nishimura-kashi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html && cp -R src dist/src && touch dist/.nojekyll",
    "dev": "python3 -m http.server 5173",
    "preview": "cd dist && python3 -m http.server 4173"
  }
}
