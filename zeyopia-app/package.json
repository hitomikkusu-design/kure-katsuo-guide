{
  "name": "zeyopia-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html && cp -R src dist/src && cp -R public/. dist/ && node -e \"const fs=require('fs'),f='dist/sw.js',v='zeyopia-app-'+Date.now();fs.writeFileSync(f,fs.readFileSync(f,'utf8').replace(/zeyopia-app-v[\\w-]*/,v))\"",
    "dev": "python3 -m http.server 5174",
    "preview": "cd dist && python3 -m http.server 4174"
  }
}
